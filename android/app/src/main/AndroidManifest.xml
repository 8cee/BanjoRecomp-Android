<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="false"
        android:hasCode="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:appCategory="game"
        android:isGame="true">
        <activity
            android:name="io.github.banjorecomp.DiagnosticsActivity"
            android:exported="false"
            android:label="@string/diagnostics_title"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/BanjoTheme" />
        <provider
            android:name="io.github.banjorecomp.DiagnosticsFilesProvider"
            android:authorities="${applicationId}.diagnostics"
            android:exported="false"
            android:grantUriPermissions="true" />
        <activity
            android:name="io.github.banjorecomp.ModBrowserActivity"
            android:exported="false"
            android:label="8CEE Mod Server"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/BanjoTheme" />
        <activity
            android:name="io.github.banjorecomp.BanjoSDLActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:label="Banjo Recompiled"
            android:launchMode="singleTask"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/BanjoTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
    </application>
</manifest>
